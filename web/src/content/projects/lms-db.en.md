Co-developed a responsive ASP.NET Core MVC web application using Entity Framework Core for relational mapping with a MySQL database.

Wrote custom database queries using LINQ joins and left joins to fetch course catalogs, class listings, and student submissions.

Designed and implemented scheduling validation logic to prevent room booking conflicts and block duplicate course enrollments in the same semester.

Built a rolling grade calculator that computes weighted category scores and translates final percentages into GPAs using custom memory lookups.

Wrote automated unit tests using xUnit and EF Core's InMemory database provider to verify controller endpoints and business logic.
